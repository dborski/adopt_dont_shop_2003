<h1>Pets</h1>

<% @pets.each do |pet| %>
  <h2><%= pet.name %></h2>
  <img src=<%= "#{pet.img_url}" %> alt=<%= "#{pet.id}" %> height="100" width="100">
  <p><%= pet.age %></p>
  <p><%= pet.sex %></p>
  <p><%= pet.shelter.name %></p>
<% end %>
